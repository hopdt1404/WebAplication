const name = document.getElementbyId('name')
const form = document.getElementbyId('form-signin')
const address = document.getElementbyId('address')

// form.addEventListener('submit', (e) => {
// 	e.preventDefault()
// })

/* list const of form*/

/*
	keydown
*/



 // document.addEventListener('keydown', function (event) {
	//   if (event.keyCode === 13 && event.target.nodeName === 'INPUT') {
	//     var form = event.target.form;
	//     var index = Array.prototype.indexOf.call(form, event.target);
	//     form.elements[index + 1].focus();
	//     event.preventDefault();
	//   }
	// }); 

// 	  function submit(form) {
// 	  	var TestVar = form.name.value;
//     	alert ("You typed: " + TestVar);
// 	  }

// // 	function capitalizeFirstLetter(string) {
// //   		return string.charAt(0).toUpperCase() + string.slice(1);
// // 	}

// // 	document.addEventListener('keydown', function (event) {
// // 		if ((event.keyCode === 13 || event.keyCode === 9) && event.target.getAttribute("name") === "name" ) {
// // 			console.log("ok");
// // 			var name = event.target.getAttribute.textContent;
// // 			var elementsName = name.split(' ');
// // 			name = '';
// // 			var i;
// // 			for (i = 0; i < elementsName.length; i++) {
// // 				name += capitalizeFirstLetter(elementsName[i]) + " ";
// // 			}

// // 			event.target.setAttribute("value", name);
// // 		}
// // 	})
// // ;	    

// 	    